<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="{{ asset('bundles/front/css/custom.css') }}">
    </head>
    <body>
        <nav class="navbar navbar-inverse navbar-fixed-top">
            <div class="container">
                <div class="navbar-header">
                    <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                        <span class="sr-only">Toggle navigation</span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                        <span class="icon-bar"></span>
                    </button>

                </div>
                <div id="navbar" class="navbar-collapse collapse">
                    <ul class="nav navbar-nav">
                        <li><a>Welcome, to application</a></li>
                        <li {% if app.request.pathInfo == "/" %}class="active"{% endif %}><a href="{{ path('homepage') }}">Home</a></li>
                        <li {% if app.request.pathInfo == "/contact" %}class="active"{% endif %}><a href="{{ path('front_contact') }}">Contact</a></li>
                    </ul>
                    <ul class="nav navbar-nav pull-right">
                        <li {% if app.request.pathInfo == "/login" %}class="active"{% endif %}>
                            <a href="{% if app.user %}
                                        {{ path('logout') }}
                                     {% else %}
                                        {{ path('login') }}
                                     {% endif %}
                            ">
                                {% if app.user %}
                                    Logout
                                {% else %}
                                    Login
                                {% endif %}
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <main role="main" class="container main-layout-block">
            <div class="row">
                <div class="col-md-8 blog-main">
                    {% block body %}{% endblock %}
                </div><!-- /.blog-main -->


                {% block aside %}
                    {% include '@Front/layout.aside.html.twig' %}
                {% endblock %}
            </div><!-- /.row -->

        </main><!-- /.container -->
    </body>
    <footer class="blog-footer">
        <p>Blog template built for <a href="https://getbootstrap.com/">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
        <p>
            <a href="#">Back to top</a>
        </p>
    </footer>
</html>
